spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      username: ${USERNAME}
      password: ${PASSWORD}
    url: ${DB_URL}

